# Specifications

- [Effect EventLog Identity Root Secret](./effect-eventlog-identity-root-secret.md) - Simplifies `EventLog.Identity` to a two-field shape backed by a single root secret, with signing keys derived internally and server-side auth semantics preserved.
