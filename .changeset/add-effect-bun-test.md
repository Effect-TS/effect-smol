---
"@effect/bun-test": minor
---

Add `@effect/bun-test` package — same API surface as `@effect/vitest`
(`it.effect`, `it.live`, `it.layer`, `it.prop`, `flakyTest`, …) but backed by
Bun's native `bun:test` runner. Closes Effect-TS/effect#5964.
